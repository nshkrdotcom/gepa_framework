# GEPA Framework Onboarding

First command:

```bash
mix deps.get && mix ci
```

This repo is a single root Mix project. It exposes standalone framework
contracts for deterministic local GEPA runs and leaves governed platform
execution to later Mezzanine and Jido Integration phases.
